export class URI {
  static parse(uri) { return new URI(uri); }
  static file(path) { return new URI(path); }
  static joinPath(...segments) { return new URI(segments.join('/')); }
  constructor(uri) { this.scheme = uri.includes('://') ? uri.split('://')[0] : 'file'; this.path = uri.includes('://') ? uri.split('://')[1] : uri; this.fsPath = this.path; }
  toString() { return this.scheme + '://' + this.path; }
  toJSON() { return this; }
}
export default { URI };
